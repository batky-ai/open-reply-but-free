# open-reply-but-free — environment template
#
# Copy to .env and fill in. One line per variable, value blank, with a comment
# naming exactly where the value comes from. .env is gitignored; never commit it.

# EXAMPLE_API_KEY=          # Provider dashboard -> Settings -> API keys
